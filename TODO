- Aus Komplexitätsgründen habe ich die Implementation jetzt so geschrieben, dass die Rune-Positions überall wo diese gebraucht werden neu berechnet werden. Stellt sich heraus, dass das unglaublich unperformant ist. Sobald ich die ds/map so erweitert habe, dass ich Pointer als Keys benutzen kann, werde ich eine Hashmap von Piece-Pointern auf deren Glyph-Positions erstellen, welche die Positionen cached und die dann nur noch aktualisieren, wenn das Piece sich geändert hat.
